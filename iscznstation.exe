import tkinter as tk
import webbrowser

def pesquisar(event=None):
    termo = entrada.get()
    if termo:
        url = f"https://www.google.com/search?q={termo}"
        webbrowser.open(url)

# Criar janela principal
janela = tk.Tk()
janela.title("ISCZN Station - Pesquisa")
janela.geometry("600x150")
janela.configure(bg="#f2f2f2")

# Título
titulo = tk.Label(janela, text="🔎 ISCZN Station - Pesquisa Google", font=("Arial", 16), bg="#f2f2f2")
titulo.pack(pady=10)

# Campo de entrada
entrada = tk.Entry(janela, font=("Arial", 14), width=50)
entrada.pack(pady=5)
entrada.bind("<Return>", pesquisar)

# Botão de pesquisa
botao = tk.Button(janela, text="Pesquisar", font=("Arial", 12), command=pesquisar)
botao.pack(pady=5)

janela.mainloop()
